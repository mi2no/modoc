## Overview
`@style` is a special node used to define the styling of a document.
It relies on css-like declarations. Example:
```
@style
    :id
        background: blue;
        color: white;

    sec
        color: yellow;
```
`@style` is optional, but can only appear once, on the top of a document.

## Syntax

